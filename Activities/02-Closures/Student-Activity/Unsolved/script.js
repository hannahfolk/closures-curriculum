function outerFunction(arr) {
  function innerFunction(target) {
    // if the target is in the arr, return the index of that target
    // if not, return -1
  }
  // return the innerFunction
}

// create a closure by passing in an array to the outerFunction and setting it to a variable
// call the closure and pass in the target
